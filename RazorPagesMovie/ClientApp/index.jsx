import React from 'react';
import ReactDOM from 'react-dom';

function App() {
  return (
    <div>
      react is running <div>npm start + dotnet watch run</div>
    </div>
  );
}
ReactDOM.render(<App />, document.getElementById('root'));
